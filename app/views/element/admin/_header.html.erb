<nav class="navbar navbar-default">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbarEexample7">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<% if logged_in? %>
				<%= link_to '管理画面',staffs_path ,class: 'navbar-brand'%>
			<% else %>
				<a class="navbar-brand" href="#">
					管理画面
				</a>
			<% end %>
		</div>
		<% if logged_in? %>
			<div class="collapse navbar-collapse" id="navbarEexample7">
				<ul class="nav navbar-nav navbar-right">
					<li class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"><%= current_staff.name %> <span class="caret"></span></a>
						<ul class="dropdown-menu" role="menu">
							<li><%= link_to '基本情報編集' , edit_staff_path(current_staff.id) %></li>
							<li><%= link_to 'ログアウト' , staff_logout_path , method: 'delete' %></li>
						</ul>
					</li>
				</ul>
			</div>
		<% end %>
	</div>
</nav>
